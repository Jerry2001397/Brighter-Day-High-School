# Database Configuration (PostgreSQL)
# For local development, use: postgresql://username:password@localhost:5432/database_name
# For Render.com, this will be provided automatically as DATABASE_URL
DATABASE_URL=postgresql://postgres:password@localhost:5432/brighter_day_school

# Server Configuration
PORT=3000
NODE_ENV=development

# Session Secret (CHANGE THIS to a random string in production!)
SESSION_SECRET=your_secret_key_here_change_in_production

# Admin Default Credentials (For reference only - actual credentials set in database)
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
